const startConversation = (stream) => conversation

const endConversation = (stream) => conversations

const askQuestion = (conversation, questionJSON) => question

const receiveQuestion = (responseStream, questionJSON) => question

// const respondToQuestion = (responseStream, responsesJSON) => true

const receiveResponse = (questions, responsesJSON) => response
